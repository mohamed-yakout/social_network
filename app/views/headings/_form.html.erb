<%= form_with(model: heading, local: true) do |form| %>
  <% if heading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(heading.errors.count, "error") %> prohibited this heading from being saved:</h2>

      <ul>
      <% heading.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :html_element %>
    <%= form.text_field :html_element %>
  </div>

  <div class="field">
    <%= form.label :html_value %>
    <%= form.text_field :html_value %>
  </div>

  <div class="field">
    <%= form.label :html_type %>
    <%= form.text_field :html_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
