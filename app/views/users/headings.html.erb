<h1>Headings</h1>

<table>
  <thead>
    <tr>
      <th>Html value</th>
      <th>Html type</th>
    </tr>
  </thead>

  <tbody>
    <% @headings.each do |heading| %>
      <tr>
        <td><%= heading.html_value %></td>
        <td><%= heading.html_type %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @headings %> |

<%= link_to 'Back', params[:logined_user_id] ? home_user_path(params[:logined_user_id]) : user_path(@user) %>
